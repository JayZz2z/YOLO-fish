# YOLOfish：训练入口 `train_fsdnet_v8.py` 通过 `sys.path` 使用仓库内 `ultralytics-main/`，
# 不要求 `pip install ultralytics`。若希望任意目录下 `import ultralytics`，可执行：
#   pip install -e ./ultralytics-main
#
# 下列版本来自本机当前 Python 环境的 `pip show`（抽样：Python 3.13 / Windows）。
# 新建环境时：请按显卡与驱动从 https://pytorch.org 选择匹配的 torch/torchvision 轮子，
# 不必强求与下述 torch 小版本完全一致。

# ---- PyTorch（按官方指引安装时可注释掉下面两行，改用自己的 CUDA 源）----
torch==2.9.1
torchvision==0.24.1

# ---- Ultralytics 核心依赖（与 ultralytics-main/pyproject.toml 对齐，并 pin 当前环境版本）----
numpy==2.3.5
matplotlib==3.10.6
opencv-python==4.13.0.92
pillow==12.0.0
PyYAML==6.0.3
requests==2.32.5
scipy==1.16.3
psutil==7.0.0
polars>=1.0.0
ultralytics-thop>=2.0.18

# ---- 常用训练/评估增强（当前环境已装，COCO 类指标更快）----
faster-coco-eval==1.7.1
